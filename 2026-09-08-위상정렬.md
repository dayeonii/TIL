# 위상정렬 Topology Sort (Kahn’s Algorithm)

상태: 완료
선택: 그래프, 위상정렬

## 위상 정렬 알고리즘 Topological Sort

- **순서가 있는 일련의 작업**을 차례대로 수행해야 할 때
- **사이클이 없는 방향 그래프**의 모든 노드를 순서대로 나열하는 것
    - 사이클이 있으면?
    - 위상 정렬의 전제는 **선수과목을 들어야 후수과목을 들을 수 있다**인데,
    - 사이클이 있다면 모순 관계가 발생함 (a.k.a 닭이 먼저냐 계란이 먼저냐)

⇒ 방향이 있고 사이클이 없는 그래프 Directed Acyclic Graph에서만 가능하다

<aside>
👉

위상정렬이 안되는 그래프 → 사이클이 존재한다고 판단

- indegree 방법에서 큐에 들어간 노드 수 == 그래프의 노드 수 인지 확인
- 사이클이 있으면 모든 노드의 indegree가 0이 될 수 없음
</aside>

- **여러가지 가능한 답** 중 하나를 뽑아준다

#### 진입차수 Indegree / 진출차수 Outdegree

- 진입차수 :특정 노드로 들어오는 간선의 개수
- 진출차수 : 특정한 노드에서 나가는 간선의 개수

### 구현 방법

#### DFS + Stack

- dfs로 탐색을 진행하다가 더 이상 진행할 수 없는 노드가 되면
- 그 순서를 스택에 기록함
- 모든 노드에 대해서 탐색이 진행되면
- 스택에서 순서대로 값을 빼내면 → 위상정렬 순서가 된다
- 모든 노드에 대해서 방문했는지 확인해줘야 한다
    
    ![image.png](%EC%9C%84%EC%83%81%EC%A0%95%EB%A0%AC%20Topology%20Sort%20(Kahn%E2%80%99s%20Algorithm)/image.png)
    
    - 이 그래프에서 `1` 부터 시작한다면 → `1-5-7` 을 탐색하고 끝나게 됨
    - 방문한 적 없는 노드인 `2`부터 동일한 과정을 진행해줘야 함

<aside>
👉

각 정점과 간선을 한 번씩 보게 되므로 시간복잡도 $O(V+E)$

</aside>

#### kahn’s Algorithm (indegree + Queue)

- 큐 사용
- 큐가 빌 때까지 반복
    - 진입차수가 0인 노드를 큐에 넣음
        - 진입차수가 0 : **지금 당장 할 수 있는 작업** (선수과목 다 들은 거)
    - 큐에서 원소를 꺼내 해당 노드에서 나가는 간선을 그래프에서 제거
    - 새롭게 진입차수가 0이 된 노드를 큐에 삽입
- 정답이 여러 가지 나올 수 있다

### 사용하는 자료구조 (in-degree 기법)

#### 인접리스트

- `List<List<Integer>> graph`

#### in degree 배열

- `int[] indegree`
    - 각 노드로 들어오는 간선 개수만 정하면 됨
    - 노드 번호를 인덱스로 사용해서 각 진입차수를 $O(1)$ 만에 구할 수 있음

#### Queue

- `Queue<Integer> queue = new LinkedList<>();`
    - 큐에서 데이터를 꺼낼 때, 맨 앞 데이터를 꺼내게 된다
    - LinkedList는 노드 간 연결 포인터만 바꿔주면 되므로 맨 앞 데이터 삭제 시 $O(1)$
        - 링크 타고 들어가서 노드를 찾을 필요 없이 헤드가 가리키고 있는것만 바꿔주면 되니까
- `ArrayDeque` 를 사용해도 된다
    - 배열 기반 큐 : 원형 큐 아이디어를 사용함
    - 원형 큐도 head, tail 인덱스를 사용하니까, $O(1)$ 만에 맨 앞 삭제 / 맨 뒤 추가가 가능함
    - ArrayDeque가 LinkedList보다 캐시 효율이랑 메모리 절약이 더 좋은 편이긴 하다